#ifndef AUDIOHANDLERRASPI
#define AUDIOHANDLERRASPI

void musicHandlerRaspi(int musicId); //handler de musica para raspi
void regVolumeRaspi(int sentido); //handler de volumen para raspi
void soundHandlerRaspi(int soundId); //hanlder de sonido para raspi

#endif